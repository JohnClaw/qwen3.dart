name: runq_dart
description: A Dart implementation of a quantized transformer model runner.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  typed_data: ^1.3.2

dev_dependencies:
  lints: ^3.0.0